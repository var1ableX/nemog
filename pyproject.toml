# pyproject.toml
[project]
name = "nemog"
version = "0.1.0"
dependencies = [
    "langgraph>=0.2.0",
    "typing-extensions>=4.0.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
